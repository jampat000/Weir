#!/usr/bin/env sh
# Runs the local pre-push checks (scripts/pre-push.mjs). One-time setup: git config core.hooksPath .githooks
set -e
exec node "$(git rev-parse --show-toplevel)/scripts/pre-push.mjs"
