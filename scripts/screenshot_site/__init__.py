"""Implementation package for ``scripts/screenshot-site.py``.

Split out of that one script (#747) so each part stays under the project's line-count guideline.
Nothing here is imported from elsewhere; the entry point script is the only supported way to run it.
"""
